import { {{pascalCase name}}Container as {{pascalCase name}}ard } from './{{camelCase name}}.container';

export const {{pascalCase name}}Route= {
    exact: false,
    path: {{route}}
    component: {{pascalCase name}}
}